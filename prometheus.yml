global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'spring-actuator'
    metrics_path: '/actuator/prometheus'
    scrape_interval: 5s
    static_configs:
      # 컨테이너 내부 통신을 위해 'app' 서비스 이름과 내부 포트 '8080'을 사용합니다.
      - targets: ['app:8080']